bert_src = [
    meson.current_source_dir() / 'bert_transformer.cpp',
    meson.current_source_dir() / 'multilingual_tinybert_16mb.cpp',
]

bert_inc = include_directories('.')

causallm_src += bert_src
causallm_inc += bert_inc
